
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        vim:SendKeyEvent(true, "Q", false, game)
                                        vim:SendKeyEvent(false, "Q", false, game)
                                        task.wait(0.3)
                                    end
                                end)
                                

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)


                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)

                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)




                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
                                task.spawn(function()
                                    while getgenv().farmRegularDummies do
                                        ReplicatedStorage.SkillsInRS.RemoteEvent:FireServer(currentDummy.HumanoidRootPart.Position, "NewLightningball")
                                        task.wait(0.3)
                                    end
                                end)
